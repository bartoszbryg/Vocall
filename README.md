# SAGA

SAGA is a multi-agent voice narrative platform that uses temporal Graph Neural
Networks to maintain a living, time-aware memory of an evolving story. Voice
input is transcribed, parsed into graph nodes and edges representing characters
and relationships, and fed to a set of specialized AI agents that generate
narrative responses — spoken back to the user in real time.